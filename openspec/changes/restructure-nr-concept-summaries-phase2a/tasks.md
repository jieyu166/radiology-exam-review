<!--
Each task states an observable contract and its verification target. Batches are
sequential by design; no task is marked [P].
-->

## 1. Production batch 基礎與完整排程

- [x] 1.1 實作「4. 驗證契約是 checkout-path independent，CLI 不可依目前檔案位置推論資料位置」、「Stable failure modes」與「Scoped concept build supports safe production batches」：以 TDD 擴充 `scripts/nr_summary_audit.py`、`scripts/test_nr_summary_audit.py`、`scripts/build_concepts.py`、`scripts/test_build_concepts.py`，提供 design 所列 Phase 2 assignment／baseline／batch／generated-manifest 公開介面與 CLI；所有 JSON path 為 repo-relative POSIX path，coherent shadow checkout 與 canonical checkout 產生相同 finding codes、digests 與 exit codes，scoped build 在任何失敗前不得寫檔。以完整 audit/build tests、`py_compile`、canonical/shadow attack regressions 及既有 Phase 1 suites 全綠驗證，且不得修改任何 concept Markdown。
- [x] 1.2 建立「1. 所有非 pilot note 由 deterministic assignment 排程，active 與 scheduled 明確分離」與「Phase 2 assignment is complete, deterministic, and activates only the Phase 2A tranche」：產生並驗證 `docs/reports/nr-summary-rewrite/phase2-assignment.json`，精確涵蓋 206 non-pilot notes，固定三個 10-note active batches，依 type／ASCII slug 將其餘 176 筆排入 type-homogeneous `scheduled-not-started` batches，並同步 `inventory.json` 而不改 Phase 1 pilot hashes。以 `validate-assignment` 報告 216 total／10 pilot／206 non-pilot／30 active／176 scheduled、重新產生 bytes 相同、固定 slug substitution 與 duplicate/missing/type drift regressions 全部被拒絕來驗證。

## 2. `batch-01-anatomy`

- [x] 2.1 建立「2. baseline lock 使用每批一個中央程式信任摘要，而非每 note trust constant」、「3. report 與 lock 分層；證據、處置與手動佇列可導出而不可自我宣稱」與「Each active batch has a lossless baseline lock and independently trusted digest」：在任何 Summary edit 前，為固定 10 個 anatomy/measurement/management slugs 建立 lossless baseline lock、stable fact units、evidence scaffold 與單一 code-owned batch digest；lock／assignment／inventory／目前 source hash 與 Summary snapshot 必須一致，coordinated lock/report mutation 必須失敗。以 `validate-baseline --batch batch-01-anatomy`、逐 note hash/snapshot comparison、trusted-digest mutation regression 及 git diff 證明本 task 未改 concept Markdown。
- [x] 2.2 實作「Evidence reports provide complete, source-defined fact dispositions」與「Literature research is exception-only, auditable, and access-safe」：由獨立 implementer subagent 僅改寫 `batch-01-anatomy` 的 10 個 Summary，以 anatomy/measurement/management 的 sourced labels 保存所有 fact qualifiers、numbers、versions、negations、exceptions 與 Obsidian footnotes；只對觸發條件使用 `radiology-topic-research`，無法取得的來源保留 derived manual queue。以 10/10 strict `validate-note`、fact/source/footnote coverage、`newUnsupportedFacts=0`、batch-scoped build 與 evidence report 驗證，且正文、frontmatter、題目、圖片、Dataview 與其他 notes 零差異。
- [x] 2.3 滿足「5. 三批次是 sequential subagent/reviewer gate，不是三個可平行執行的工作項」、「6. generated output 以 batch-scoped manifest 驗證寫入邊界與目前 corpus coherence」、「Batch workflow enforces independent implementation and review gates」、「Batch-scoped generation is coherent, narrow, and idempotent」及「Every completed Phase 2A batch has an auditable generated-output manifest and rejects nonselected drift」：由不同 reviewer subagent 審查 `batch-01-anatomy` 的原始 lock、逐 fact 來源、Summary diff、manual queue 與 generated output；修正所有 Critical/Important findings 後才記錄 approved。以 reviewer identity 不同、batch validator 無非 manual findings、完整 generated manifest、978-entry corpus coherence、連跑兩次零 bytes/mtime/Git drift 與 nonselected mutation attack 被拒絕驗證；未 approved 前不得開始 batch-02。

## 3. `batch-02-disease`

- [x] 3.1 在 batch-01 approved 後，依「2. baseline lock 使用每批一個中央程式信任摘要，而非每 note trust constant」為固定 10 個 disease slugs 建立 pre-edit baseline lock、stable fact units、evidence scaffold 與單一 trusted batch digest；以 `validate-baseline --batch batch-02-disease`、source hash／lossless snapshot、coordinated mutation regression 及無 Markdown diff 驗證。
- [x] 3.2 依「Evidence reports provide complete, source-defined fact dispositions」與「Literature research is exception-only, auditable, and access-safe」，由新的 implementer subagent 僅改寫 `batch-02-disease` 的 10 個 Summary；疾病介紹只使用原始來源支持的 disease axes，缺少年齡、病理、症狀或影像類別時直接省略，不得仿造 Alzheimer 圖而補出未來源事實。以 10/10 strict validation、逐 fact/source/footnote coverage、`newUnsupportedFacts=0`、batch-scoped build 與 scope diff 驗證。
- [ ] 3.3 依「Batch workflow enforces independent implementation and review gates」、「Batch-scoped generation is coherent, narrow, and idempotent」與「Every completed Phase 2A batch has an auditable generated-output manifest and rejects nonselected drift」，由不同 reviewer subagent 完成 disease batch 的規格、醫學證據及 generated-output 審查；以 findings 關閉、derived manual queue、approved review record、兩次零漂移 build、完整 manifest／corpus coherence 與 nonselected protection 驗證，未 approved 前不得開始 batch-03。

## 4. `batch-03-pattern`

- [ ] 4.1 在 batch-02 approved 後，依「2. baseline lock 使用每批一個中央程式信任摘要，而非每 note trust constant」為固定 10 個 pattern-ddx slugs 建立 pre-edit baseline lock、stable fact units、evidence scaffold 與單一 trusted batch digest；以 `validate-baseline --batch batch-03-pattern`、source hash／lossless snapshot、coordinated mutation regression 及無 Markdown diff 驗證。
- [ ] 4.2 依「Evidence reports provide complete, source-defined fact dispositions」與「Literature research is exception-only, auditable, and access-safe」，由新的 implementer subagent 僅改寫 `batch-03-pattern` 的 10 個 Summary；依可區辨的部位、sequence/signal、分布、對稱性、enhancement、年齡/臨床、陷阱等有來源影像軸建立短 bullets，不得把未來源鑑別或關係加入矩陣。以 10/10 strict validation、逐 fact/source/footnote coverage、`newUnsupportedFacts=0`、batch-scoped build 與 scope diff 驗證。
- [ ] 4.3 依「Batch workflow enforces independent implementation and review gates」、「Batch-scoped generation is coherent, narrow, and idempotent」與「Every completed Phase 2A batch has an auditable generated-output manifest and rejects nonselected drift」，由不同 reviewer subagent完成 pattern batch 的規格、醫學證據及 generated-output 審查；以 findings 關閉、derived manual queue、approved review record、兩次零漂移 build、完整 manifest／corpus coherence 與 nonselected protection 驗證。

## 5. Phase 2A tranche acceptance

- [ ] 5.1 執行「狀態與資料不變量」、「驗證與 acceptance gates」、「分層 acceptance gates」、「Generated keyPoints aggregate every accepted Summary variant for every scoped batch note」、「Phase 2A tranche verification proves all three batches and the complete generated corpus」與「Tranche acceptance is exhaustive and does not start later work」：聚合三個 approved batches，驗證 30 unique active notes、完整 216-note inventory、三個 trusted locks/evidence/reviews/manifests、所有 Summary variants 的 keyPoints、完整 generated index/detail tree、兩個 named lint errors 且無新 error，以及每項 warning delta 有可稽核說明；確認 176 scheduled notes 與 Phase 1 evidence 零改動。以完整 audit/build/compile、三批 canonical/shadow attacks、30/30 strict notes、tranche validator、連跑兩次 scoped rebuild 零漂移、Spectra analyze/validate 與獨立 final reviewer 證明 root 僅能為 `verified` 或誠實的 `phase2a-complete-with-manual-queue`，且 Phase 2A 不啟動後續 tranche。
