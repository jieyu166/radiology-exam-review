# Task 2.3 scoped fix — review round 1

## Outcome

- Status: `DONE_WITH_CONCERNS`
- Fix base: `4431cd9`
- Finding fixed: the one Important finding in `review-task-2-3.md`
- Scope: generated predecessor acceptance before any later-batch write
- Task 2.3 checkbox remains unchanged.
- No concept content, evidence content, manifest, observation digest, or
  medical source was changed.

The only retained concern is the already documented
`brain-herniation-syndromes-f03` research queue; it is unrelated to this
workflow fix.

## RED

A production-contract regression now builds a coherent approved three-batch
fixture, makes one batch-02 selected detail stale, and attacks the immediate
batch-01 predecessor in two ways:

1. predecessor generated manifest and code-owned seal both missing;
2. genuine predecessor manifest present but code-owned seal wrong.

For each case the test snapshots the complete generated detail tree and index
bytes and mtimes before invoking the batch-02 workflow.

Before the fix:

```text
1 failed, 123 deselected
```

The missing-predecessor case returned no failure and allowed the later
workflow to proceed, reproducing the Important finding.

## Minimal production fix

`run_phase2_generated_observation_workflow()` now performs one additional
pre-write gate for any batch whose sequence index is greater than zero:

1. derive the immediate predecessor from the fixed active-batch order;
2. load that predecessor through the same explicit repository root and
   assignment path;
3. call
   `validate_phase2_batch(predecessor, check_source_hashes=False,
   check_generated=True)`;
4. if loading or any generated/content/review/chain validation fails, raise
   stable `phase2-review-sequence` before importing or calling the builder.

The error records the first predecessor finding for diagnosis. The workflow
does not accept assignment membership, approved evidence alone, a mutable
manifest claim, or an untrusted generated observation as predecessor
acceptance.

No validator rule was weakened and no batch-01 seal was recomputed.

## GREEN and attack results

The new missing/wrong predecessor regression now passes:

```text
1 passed, 123 deselected
```

Both cases return `phase2-review-sequence` and preserve exact zero drift:

```json
{
  "changedPaths": [],
  "mtimeChangedPaths": []
}
```

The existing three-batch fixture was corrected to model the legal production
sequence:

```text
batch-01 workflow -> manifest -> trusted seal
batch-02 workflow -> manifest -> trusted seal
batch-03 workflow -> manifest
```

It no longer prebuilds batch-02 or batch-03 before batch-01 acceptance.

Later-chain compatibility and attacks:

- valid contiguous `{batch-01}` predecessor permits batch-02;
- valid contiguous `{batch-01, batch-02}` predecessors permit batch-03;
- valid full three-batch historical evolution remains accepted;
- missing or wrong immediate predecessor seal fails before writes;
- skipped/missing batch-02 seal still invalidates batch-03;
- invalid later reviewer/evidence/generated trust remains rejected;
- earlier selected drift and incoherent current index remain rejected.

Focused later-chain result:

```text
7 passed, 117 deselected
```

## Preserved batch-01 bytes and trust

The genuine batch-01 workflow was rerun unchanged:

- evidence SHA-256:
  `0225a5e6c7a6fca4d2abe6abe9d73540ef186c064dc5d3b89690d0d74857f70b`;
- manifest SHA-256:
  `bd1d2be10b9045c17b3f7ff540414623b8ecee4b6f2dd38f576aa3d118fbbbdb`;
- observation/code-owned seal:
  `7adfa693cf5a178e1a393c250b1322f4e7ea4de990fdac16c0afae26f6f9cefd`;
- regenerated manifest is byte-identical;
- generated byte drift: none;
- generated mtime drift: none;
- central registry remains exactly the single batch-01 entry.

`validate-batch --check-generated` for current batch-01 still returns exit 0
and `[]`.

## Verification gates

| Gate | Result |
|---|---|
| New predecessor missing/wrong-seal regression | PASS, 1 |
| Focused later-chain compatibility/attacks | PASS, 7 |
| Task 2.3 production + predecessor focused suite | PASS, 18 |
| Complete audit/build pytest | PASS, 136 |
| Current batch-01 generated CLI | PASS, `[]` |
| Genuine batch-01 manifest/digest parity | PASS |
| Genuine batch-01 byte/mtime rerun drift | PASS, zero |
| Four-file `py_compile` | PASS |
| Full lint | Exact inherited 2 errors / 124 warnings |
| Spectra strict validation | PASS |
| Spectra analysis | 0 Critical, 0 Warning, 2 pre-existing Suggestions |
| `git diff --check` | PASS |
| Prohibited production scope | PASS, no diff |

The exact inherited lint errors remain:

```text
[footnote 未定義] ceap-classification.md 用了 [^*] 但無定義
[json 殘留 ![[...]]] 2022-264
```

Neither belongs to the selected batch.

## Changed files

- `scripts/nr_summary_audit.py`
- `scripts/test_nr_summary_audit.py`
- this fix report

There is no diff from `4431cd9` in:

- batch-01 evidence or generated manifest;
- code-owned batch-01 baseline or observation digest values;
- concept Markdown;
- selected or nonselected generated detail JSON;
- `data/concepts-index.json`;
- inventory, assignment, or baseline locks;
- Phase 1, batch-02, batch-03, scheduled-note, Spectra-task, or Phase 2B
  artifacts.
