# Task 2.3 independent task-level re-review — round 2

## Verdict

`ADDRESSED`

- Reviewed fix commit: `f7b27c2`
- Fix base: `4431cd9`
- Reviewer: `/root/phase2a_task2_3_verification`
- Prior Important findings closed: 1/1
- New Critical: 0
- New Important: 0
- Production files edited by this reviewer: none

The immediate-predecessor generated acceptance gap is closed. A later batch
now fails before builder import or invocation unless its immediate predecessor
passes complete generated validation. The valid batch-01 → batch-02 →
batch-03 sequence remains compatible.

## Prior Important finding

### Later batch could write without predecessor generated acceptance

`ADDRESSED`

`run_phase2_generated_observation_workflow()` now:

1. derives the immediate predecessor from the fixed active-batch order;
2. loads that predecessor through the current explicit repository root and
   assignment path;
3. calls
   `validate_phase2_batch(predecessor, check_source_hashes=False,
   check_generated=True)`;
4. converts any load or validation error to stable
   `phase2-review-sequence`;
5. performs this complete gate before importing `build_concepts`, taking the
   first build snapshot, or invoking the first scoped build.

This directly closes the path proven in round 1, where approved evidence and
valid baseline trust alone allowed batch-02 writes.

## Independent verification

### Missing and wrong predecessor observations

The new regression was independently rerun for:

- missing batch-01 manifest and missing observation registry entry;
- genuine batch-01 manifest with a wrong code-owned observation seal.

Both cases:

- returned `phase2-review-sequence`;
- performed zero generated byte changes;
- performed zero generated mtime changes;
- did not reach the builder call because the gate is structurally before the
  builder import and invocation.

### Valid sequential compatibility

Scoped independent pytest result:

```text
4 passed, 120 deselected
```

The four exercised paths were:

1. missing/wrong immediate-predecessor acceptance before write;
2. valid later trusted batch update preserving earlier historical scope;
3. batch-03 requiring the batch-02 generated observation seal;
4. authorized later update remaining relocation-stable.

The corrected fixture now follows the legal sequence:

```text
batch-01 workflow -> manifest -> trusted seal
batch-02 workflow -> manifest -> trusted seal
batch-03 workflow -> manifest
```

Normal continuous registry prefixes `{batch-01}`,
`{batch-01,batch-02}`, and `{batch-01,batch-02,batch-03}` remain accepted;
skipped or untrusted predecessors remain rejected.

### Current batch-01 preservation

- `validate-batch --check-generated`: exit 0, `[]`.
- Evidence SHA-256 remains
  `0225a5e6c7a6fca4d2abe6abe9d73540ef186c064dc5d3b89690d0d74857f70b`.
- Generated manifest SHA-256 remains
  `bd1d2be10b9045c17b3f7ff540414623b8ecee4b6f2dd38f576aa3d118fbbbdb`.
- The code-owned observation seal remains
  `7adfa693cf5a178e1a393c250b1322f4e7ea4de990fdac16c0afae26f6f9cefd`.
- There is no fix-commit diff in evidence, generated manifest, concept
  Markdown, detail JSON, index, inventory, assignment, baseline, Phase 1,
  batch-02/batch-03 artifacts, or Spectra task state.

## Code and test audit

The 34-line production change is narrow and fail-closed:

- it reuses the existing complete batch validator rather than creating a
  second mutable acceptance condition;
- it accepts warnings but rejects every predecessor error;
- it preserves repository-relative diagnostic paths;
- it introduces no recursive `check_generated=True` cycle because the
  predecessor's ordinary workflow-sequence check still loads its predecessor
  with `check_generated=False`, while generated-chain validation explicitly
  walks the finite contiguous prefix;
- batch-01 has index zero and is unaffected.

The 84-line regression change removes the previous out-of-order fixture setup,
adds exact missing/wrong predecessor attacks with byte/mtime snapshots, and
retains legal batch-02/03 chain coverage. It is meaningful coverage rather
than a production-value-only assertion.

`git diff --check 4431cd9..f7b27c2` passes. The fix changes only:

1. `scripts/nr_summary_audit.py`;
2. `scripts/test_nr_summary_audit.py`;
3. the scoped fix report.

## Conclusion

The sole round-1 Important finding is closed with no new Critical or
Important regression. Task 2.3 is ready for the controller's completion
decision. The existing `brain-herniation-syndromes-f03` research queue remains
an honest batch concern, not a failure of this fix.
