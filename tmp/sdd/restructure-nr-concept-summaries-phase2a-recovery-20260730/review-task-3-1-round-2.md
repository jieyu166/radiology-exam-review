# Task 3.1 independent review — round 2

## Verdict

**APPROVED**

- Fix range: `298b30b4bd6b31b144c7648b579c17620c7eb7bd..521fce6`
- Critical: 0
- Important: 0
- Suggestion: 0

All three round-1 Important findings are addressed. The regenerated lock is a
lossless 60-statement / 98-fact projection, the pending evidence remains
honestly nonterminal, and the batch-01 compatibility path remains narrow and
fail-closed.

## Round-1 findings

### 1. Footnote definitions and continuations no longer create facts

Addressed.

- `phase2_summary_source_statements` identifies a footnote definition and
  skips that definition plus its blank, four-space, or tab-indented
  continuation lines.
- The synthetic regression covers inline definition metadata, four-space
  continuation, and tab continuation.
- Independent production inspection found zero fact whose `sourceStatement`
  matches `^\[\^[^\]]+\]:`.
- ALD now contains 18 factual source statements and 36 facts instead of the
  contaminated 28/70 projection.
- Its actual rendered definitions remain available through the pending
  evidence `sourceDefinitions`; they are no longer duplicated as facts.

### 2. Aicardi triad and ALD Schaumburg-zone context are lossless

Addressed.

- Ordered nested bullets are recognized in addition to unordered children.
- A pure-label semantic parent followed by nested children is retained exactly
  once as a context fact.
- A child without an explicit footnote inherits the enclosing parent refs;
  explicit child refs remain in source order.

Production checks:

- `aicardi-syndrome-f03` is the exact
  `- **典型三主徵**：[^1][^2]` parent context with refs `1,2`.
- Aicardi `f04`–`f06` preserve the three exact numbered source lines and each
  inherits refs `1,2`.
- `adrenoleukodystrophy-f09` preserves
  `三區帶（Schaumburg zones,由中央向外）` exactly once with the child-derived
  ref union `4,3`.
- ALD `f10`–`f15` remain in central, intermediate, peripheral source order,
  with each child statement and its imaging qualifiers intact.
- Across all 98 facts, the only empty refs are the two original unreferenced
  DDx statements:
  `adrenoleukodystrophy-f30` and
  `adrenoleukodystrophy-f33`.

### 3. Baseline registry and present locks require a contiguous active prefix

Addressed.

- Registry keys must be known active batch IDs.
- Both registry IDs and present active baseline IDs must equal the first N
  entries of `ACTIVE_PHASE2A_BATCHES`.
- Batch-02-only and batch-01-plus-batch-03 gaps fail with
  `phase2-trusted-batch-lock-mismatch`.
- A complete batch-01/02/03 prefix is accepted by the shape/presence gate.
- The focused production registry remains exactly batch-01 plus batch-02; no
  batch-03 artifact or key was introduced.

## Batch-01 legacy compatibility audit

The compatibility branch is narrowly scoped:

- It is selected only inside `validate_baseline_lock` when the loaded batch ID
  is exactly `batch-01-anatomy`.
- It recomputes the immutable historical source projection; it does not accept
  arbitrary supplied templates and does not alter any checked artifact.
- Baseline digest trust remains enforced before the projection comparison.
- Source hash and lossless Summary checks remain in
  `_validate_phase2_source_state`.
- Evidence fact coverage, source definitions, integrity checksum, manual
  queue, independent reviewer snapshot, and predecessor sequencing remain in
  `validate_phase2_batch`.
- Generated manifest, generated-observation seal, selected keyPoints, full
  tree/index coherence, historical write scope, and idempotence checks remain
  unchanged.

Independent gates:

- Injecting an ungrounded batch-01 fact still returns
  `phase2-baseline-schema`.
- A wrong batch-01 code-owned digest still returns
  `phase2-trusted-batch-lock-mismatch`.
- Production batch-01
  `validate-batch --check-generated` returns exact `[]`.
- The batch-01 baseline/evidence/generated bytes and seals are unchanged.

## Deterministic artifact audit

- Notes: 10, exact assignment order.
- Statements: 60.
- Facts: 98.
- Footnote-definition facts: 0.
- Canonical batch-02 baseline digest:
  `3c294b4e098fc971ec6cbc67945cc24620752108f7d31bf3c1ba574ef6fd8fa8`.
- Baseline file SHA-256:
  `334c132f12d60c8f5eb51373a11fe97ea228d14d9c7729ee9953f1972c03e5d9`.
- Baseline bytes: 69,906.
- Evidence file SHA-256:
  `b7119473e60ad87134ec58c7d4087078ab32598b89fcf3a7b4ea897e09626477`.
- Evidence bytes: 49,204.
- Builder regeneration and checked bytes are identical through the focused
  production regression.
- `validate-baseline --batch batch-02-disease` returns exact `[]`.

Pending evidence remains honest:

- root status `baseline`;
- sequence 2, predecessor `batch-01-anatomy`;
- implementer `/root/phase2a_task3_1_impl`;
- reviewer and reviewed baseline SHA `null`;
- review status `not-started`;
- all ten note statuses and all 98 dispositions `pending`;
- no validation, coverage checksum, unsupported-fact, generated-observation,
  or approval claim.

## Scope and predecessor integrity

The fix range changes only:

- `scripts/nr_summary_audit.py`;
- `scripts/test_nr_summary_audit.py`;
- batch-02 baseline/evidence;
- the Task 3.1 fix report.

No diff exists in concept Markdown, generated detail JSON, generated index,
assignment, inventory, scheduled notes, Task 3.1 checkbox, or any batch-01
artifact.

Batch-01 artifact SHA-256 values remain:

- baseline:
  `6b05caff4e2cbd618a9c15478f914853701b4be2587af58469b013917d0a7934`;
- evidence:
  `0225a5e6c7a6fca4d2abe6abe9d73540ef186c064dc5d3b89690d0d74857f70b`;
- generated manifest:
  `bd1d2be10b9045c17b3f7ff540414623b8ecee4b6f2dd38f576aa3d118fbbbdb`.

`git diff --check 298b30b..521fce6` passes.

## Test evidence

- Focused parser, context, registry, batch-02, and legacy compatibility
  regressions: `15 passed, 123 deselected`.
- Batch-02 direct baseline gate: `[]`, exit 0.
- Batch-01 direct terminal generated gate: `[]`, exit 0.
- Full independent audit/build pytest:
  `150 passed in 108.51s`.
